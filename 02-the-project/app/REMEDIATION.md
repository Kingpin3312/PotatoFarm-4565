# Remediation backlog

From the pre-launch audit of 25 September 2026 (57/100). One row per
finding. Status is only ever changed after the fix is verified, and the
verification is named in the Test column.

Status: OPEN · IN PROGRESS · FIXED · PARTIAL · OWNER (needs a decision or
an outside party, not code) · N/A

| ID | Sev | Finding | Root cause | Solution | Files | Risk | Test | Status |
|---|---|---|---|---|---|---|---|---|
| A1 | P0 | Leads screen shows only 25 leads | Server paged by cursor; the screen never asked for page two | Infinite list, 50 at a time, loading as the end comes into view plus a "Show more" button; heading is the server count of the same filter; "select all N that match" acts on the whole match server-side | `leads/page.tsx`, `routers/leads.ts` | Medium | `check:lead-lists` pages 1/25/26/60 in three sorts with ties; `browser:lead-list` scrolls 130 in a browser; both proved red | FIXED |
| A2 | P0 | Listings screen shows only 25 | `fetchNextPage` never called; heading counted loaded rows as "live" | Same pattern; filters by status, sale/rent, area (aliases), agent, price range, bedrooms; four sorts; `listings.count` for the heading | `listings/page.tsx`, `routers/listings.ts` | Medium | `check:lead-lists` (listings half), proved red | FIXED |
| A3 | P0 | No binding Terms, Privacy Policy, DPA | Legal text not written | Counsel drafts; the product then records acceptance and version at sign-up | `website/legal.html`, signup | Low | — | OWNER |
| A4 | P0 | No mapped import; no manager export | The only import recorded issues and never created a lead; export existed only for one blackbook | `/leads/import`: read any CSV (quoted commas, line breaks, BOM, semicolons), columns guessed from headers and correctable, preview with a verdict per row (new / already here / repeat of line N / error with reason), import in batches tagged for undo, "fill blanks" never overwrites, requirements from areas/beds/budget; Export on leads and listings honouring filters, managers only (`lead:import`, `lead:export`), formula cells neutralised, every import/export audited | `lib/csv.ts`, `lib/import-fields.ts`, `server/lib/import/plan.ts`, `routers/imports.ts`, leads/listings routers and pages | Medium | `check:import-export` + `browser:import-leads` + unit tests, each proved red | FIXED |
| B1 | P1 | Phone search only matches exact E.164 | Digits parsed as money first; raw substring compare | One `lib/phone.ts` (three copies before); phone-like runs read before the budget; matched on the national number in search, owners and the leads list | `lib/phone.ts`, `search/parse.ts`, `search/run.ts`, `routers/leads.ts` | Low | `check:search` five typed forms + owner, proved red by removing the clause; unit tests | FIXED |
| B2 | P1 | Lead form rejects spaced/local numbers; raw JSON errors | Strict regex on input; `e.message` was the serialised issue list | Server normalises every written form to E.164; the tRPC error formatter puts the first issue's sentence in `message`, which every form already shows | `routers/leads.ts`, `api/trpc.ts`, `leads/add-lead.tsx` | Low | `browser:add-lead`, proved red with the old regex and with the old formatter | FIXED |
| B3 | P1 | Lead list tools too thin | Four presets, fixed sort, assign-only bulk | Filter bar (search, source, score band, stage, agent, tag), five sorts, tags, saved views (own or team), bulk assign / stage / tag / untag / archive / delete / restore on the ticked rows or everything matching (capped at 5,000), export of the filtered list | leads router + page, `SavedView`, `Lead.tags`, `views` router | Medium | `check:lead-lists`: every filter, bulk scope per role, audit entry per action; `check:import-export` for export | FIXED |
| B4 | P1 | Delete cannot be undone; no archive | Only `deletedAt` | `Lead.archivedAt` (any agent, own book); managers' "Recently deleted" view with Restore | schema, leads router, page | Low | `check:lead-lists` archive/unarchive, delete/restore, agent refused the drawer | FIXED |
| B5 | P1 | Data model fits secondary sales only | Listing thin; no rentals/off-plan; person = one opportunity | Additive (`20261005090000_listing_types_tenancies`): listing type, ready/off-plan, handover, developer, project, payment plan, unit, furnishing, rent cheques, deposit, service charge; requirements take types and ready/off-plan and the matcher enforces both; `Tenancy` with a renewal task before the 90-day notice line. **And one person, several pieces of business** (`20261008090000_opportunities`): the lead stays the person's main business so every screen, report and job reads what it did; each further one — the buyer also letting their villa — is an `Opportunity` with its own column on the same board, agent, value and close, added and moved from the person's page. A lettings agent working one can open the person read-only; the lead stays its own agent's to change. In the subject access file and cleared by erasure | schema, listings, requirements, matcher, `lib/tenancy`, `routers/opportunities.ts`, pipeline board, person page, privacy, seed | Medium | `check:listing-model`; `check:opportunities` (24 assertions: hand-off rules, two columns, lettings agent reads but cannot change the lead, colleague and viewer and rival brokerage refused, won on its own, audit, export, erasure — each guard red when removed) | FIXED |
| B6 | P1 | No portal publishing | No `Publisher` without partner agreements | Agreements are the owner's; feed remains the path | `portals/` | — | — | OWNER |
| B7 | P1 | Assistant cannot reply by itself | Owner chose drafts | Decision for the owner; measurement already collected | `assistant/run.ts` | — | — | OWNER |
| B8 | P1 | No native mobile app | `mobile/` cannot build | The web app is the phone product: installable (manifest, service worker), bottom navigation, filters behind a button so lists start on the first screen, tasks added and ticked with a thumb, and now Call / Message / Next step as the first thing under a person's name. The native Expo app is still not buildable — a separate decision (D4) | person page, `button.tsx`, `scripts/mobile-agent.mjs` | Low | `browser:mobile-agent` at 390×844 (no sideways scroll on five screens, quick actions within the first screen — proved red) | PARTIAL |
| B9 | P1 | Sweep stops at 5,000 leads / 500 listings; ~5 queries per lead | Hard `take` caps; per-lead round trips | Pages of 1,000 through every lead; last week's score in one `DISTINCT ON` per page; dismissals and open follow-ups once per brokerage; each page's scores in one batch; every live listing (`liveBook`), split by purpose once | `intelligence/sweep.ts` | Medium | `check:intelligence` at 1,050 leads and 520 listings (1.6 s), proved red for one page only, no previous scores, newest 500 | FIXED |
| B11 | P1 | Buyer requirements are never captured | Only voice intake wrote a `Requirement`; the assistant's extraction had no areas or bedrooms, and its prompt named none of the fields it parses; the matcher compared areas by exact text | Agents record them on the person page (`requirements` router); extraction names its shape and keeps an assistant requirement current until an agent saves one; areas canonicalised; matcher compares places, not strings; seed gives all 42 demo buyers one | `routers/requirements.ts`, `blackbook/[leadId]/requirements.tsx`, `lib/requirements/save.ts`, `assistant/extract.ts`, `assistant/run.ts`, `matching/score.ts`, `places.ts`, seed | Medium | `check:requirements` (search and matcher find the buyer, alias meets listing spelling, scope, assistant never overwrites an agent), each proved red at its line | FIXED |
| B10 | P1 | Email and calendar not connected | No OAuth flow; no token refresh; Gmail reading unwritten (it threw) | **Email built; only the app registrations are the owner's.** Settings → Email connects Google or Microsoft (`/api/oauth/<provider>/start` → callback) with read-mail-only scopes; the state is signed and the person finishing must be the one who started; tokens sealed in the vault and refreshed before they expire; Gmail read through history and metadata-only fetches, Outlook through `$delta`; only mail with a known client is kept (headers and a snippet); a withdrawn grant says "disconnected" on the screen; disconnect forgets the key. Two-way calendar sync is not built (the read-only calendar feed remains) | `lib/email/providers.ts`, `lib/email/sync.ts`, `api/oauth/*`, `routers/email.ts`, `settings/email`, `.env.example`, CI | Medium | `check:email-connect` against loopback Google and Microsoft (17 assertions; colleague's and forged links, refresh, known-people filter, disconnect scope each proved red) | PARTIAL |
| C1 | P2 | No spelling tolerance | `contains` only | pg_trgm word similarity ≥ 0.5 on lead and owner names, inside a scoped transaction; ranked below an exact name and labelled "name is close to" | `search/run.ts` | Low | `check:search` (Stephan→Stephen, Rashed→Rashid, exact outranks close), proved red at threshold 0.99 | FIXED |
| C2 | P2 | References must be exact | No normalisation | `AR508`, `ar 508`, `AR-508` read as one reference before the budget can take the digits; the listing ranks first, its owner second | `search/parse.ts`, `search/match.ts`, `search/run.ts` | Low | `check:search` four spellings; unit tests | FIXED |
| C3 | P2 | "buyers in dubai marina" returns 0 | The query was read correctly; nobody had a requirement (B11) | Fixed by B11 | — | — | `check:requirements` asks exactly this | FIXED |
| C4 | P3 | "villa" matches "Village" | Substring match used to decide a match | Candidates still fetched with `contains`; a candidate only scores when the term is a whole word or takes an ending (villas, relocating) | `search/match.ts`, `search/run.ts` | Low | `check:search`, proved red with substring scoring; unit tests | FIXED |
| C5 | P2 | 49 of 66 tenant tables lack FK to Organisation | Relations never declared | 50 keys added `NOT VALID` (`20261004090000_org_foreign_keys`): CASCADE, except RESTRICT on records the law keeps (invoices, KYC files and documents, screenings, beneficial owners, compliance reports). `check:tenancy` now requires a key on every `orgId` table; `migrations.py` now fails on a dropped constraint. Existing orphans (left in the dev database by old test clean-ups) not validated or deleted | schema, migration, `tenant-isolation.check.ts`, `migrations.py`, billing check clean-up | Medium | `check:tenancy` and `migrations.py`, both proved red | FIXED |
| C6 | P2 | Pink text / white-on-pink below AA | Owner palette decision | Keep colours; AA-large button labels considered | tokens | Low | `browser:a11y` | OWNER |
| C7 | P2 | Tasks are personal reminders only | `FollowUp` narrow: no author, no link to a property, no list | Additive (`20261006090000_task_delegation`): who asked, linked property; `/tasks` with Mine / I asked / Team (team for managers), done and reopen; a manager can give a task to an agent (`lead:assign`), an agent cannot give one upward; add a task from the person page; Today links to the full list | `routers/tasks.ts`, `tasks/page.tsx`, `blackbook/[leadId]/task.tsx`, nav, today | Medium | `check:journeys` (delegation both ways, tick-off leaves Today), `browser:mobile-agent`; paging bug found and fixed on the way (below) | FIXED |
| C8 | P2 | No second factor for owners/admins | Magic link only; the session columns for a device list existed and nothing wrote them | Optional two-step sign-in (authenticator-app codes, RFC 6238, written in-house and tested on the RFC vectors) for everyone, asked of owners and admins on Settings → Security: key sealed with `SECRETS_KEY`, one step of clock drift, a used code refused, ten one-time recovery codes stored as hashes, five tries per 15 minutes. Per session: a device with the email link but not the code reaches no API (`orgProcedure`) and no screen (redirected to `/sign-in/two-step`). "Where you're signed in" with sign out one / all others; last-used time, address and device now recorded. Additive migration `20261007090000_two_step`. Not compulsory — that is a brokerage's call | `lib/auth/totp.ts`, `routers/security.ts`, `api/trpc.ts`, `api/context.ts`, `auth/config.ts`, `(app)/layout.tsx`, `settings/security`, `sign-in/two-step` | Medium | `check:two-step` over HTTP with real cookies (27 assertions; token, API gate, redirect, cross-user sign-out, replay, recovery reuse each proved red at its line) + 7 unit tests | FIXED |
| S1 | P1 | *Found while fixing C8.* `/api/auth/session` returned the session token to any script on the page | The session callback returned the whole row Auth.js passed it, token and user row included — defeating the `httpOnly` cookie; with two-step it would also have carried the sealed key | The callback returns a named shape: user id/name/email/image, expiry, the row id and the two-step state | `auth/config.ts` | Low | `check:two-step` asserts the token and user row are absent, proved red by restoring the old callback | FIXED |
| S2 | P1 | *Found in the full check run.* "STOP" sent in the same second as another message could still get a reply drafted | Messages were ordered by `sentAt` alone, and WhatsApp stamps whole seconds, so which of two same-second messages was "latest" was up to Postgres; `check:reply-drafts` had been passing by luck of row order | `id` (a cuid, creation-ordered) breaks the tie wherever message order decides behaviour: the assistant's history, replay, the thread, notifications, digests, the timeline, the privacy export | `assistant/run.ts`, `assistant/replay.ts`, `routers/conversations.ts`, `notify/*`, `blackbook/timeline.ts`, `privacy/export.ts` | Low | `check:reply-drafts` 3/3 green with the tiebreak, red 1 run in 2 without it | FIXED |
| C9 | P2 | No end-to-end journey test | Suites are per workflow | `check:journeys` drives journeys 1–4 through the real procedures (new lead → match → follow-up; lead → viewing → offer → deal → won; an agent's day; a manager), `browser:mobile-agent` drives journey 5 at 390×844. Found four real faults: a completed deal left the buyer open, and confirming a viewing, recording an offer and a no-show all left the board stage where it was (`lib/pipeline/advance.ts`, forward-only) | `scripts/journeys.check.ts`, `scripts/mobile-agent.mjs`, `lib/pipeline/advance.ts`, viewings/offers/deals routers | Low | themselves; each stage assertion red before its fix | FIXED |
| C10 | P2 | Scale beyond 5,000 unmeasured | Load suite sized at 5,000 and timed raw queries only | `SCALE=5 npm run check:load` builds 25,000 leads, 6,000 listings, 32,500 requirements, 200,000 messages in 23 s, and now also times the screens through their procedures (lists with filters and sorts, deep cursor, counts, KPIs, Today) and the nightly sweep. Every agent wait is inside budget (slowest warm 151 ms, search). **It found the sweep did not finish**: every requirement was compared with every listing through `samePlace`, which rebuilt and re-sorted the whole alias table on each call — over four minutes of CPU and still running. The alias table is built once, each text's place is remembered, and `best()` is one pass: the sweep now takes 55 s for the whole database | `scripts/load.check.ts`, `lib/places.ts`, `matching/score.ts` | Low | the load run itself; unit and requirements suites unchanged | FIXED |
| C11 | P2 | Missing management KPIs | Reports incomplete | `reports.kpis` (managers): conversion by source, median days to close, weighted pipeline by stage, expected commission, cold leads by agent — each figure links to the filtered leads list, which reads its filters from the address | `routers/reports.ts`, `reports/kpis.tsx`, `leads/page.tsx` | Low | `check:journeys` (manager), `browser:screens` | FIXED |
| D1 | P3 | Blackbook capped at 300 | `take: 300`, and the `q` it accepted was never read | Paged by cursor with a total, search actually applied, "Show more" on the page. Also fixed: the cursor skipped the first row of every next page, losing one contact per 100 (same fault in `tasks.list`) | `routers/blackbook.ts`, `routers/tasks.ts`, blackbook page | Low | `check:lead-lists` 320 entries none twice (red at 317 before the skip fix), search, colleague scope | FIXED |
| D2 | P3 | Upload type trusted from browser | No content sniff | On confirm, the first bytes are read back from storage and must match the declared type (PDF, JPEG or PNG — the only types accepted at all); the stored size must match; a mismatch deletes the object and refuses. Also fixed: size read as 0 when storage sent no Content-Length | `files/signature.ts`, `files/storage.ts`, `files/upload.ts` | Low | unit tests + `check:storage`, proved red | FIXED |
| D3 | P3 | Demo data ages | Seed dates fixed at seed time | Every run re-states message times and time-in-stage relative to today | seed | Low | seed run; 14 of 36 open cards past their stage limit after a re-seed (was all) | FIXED |
| D4 | P3 | Unbuildable / unused code | `mobile/`, `email/sync`, `respond()` | Documented; decision with B7/B8/B10 | — | — | — | OWNER |
| D5 | P2 | No dependency scan in CI | Not configured | `npm run audit:deps` fails on any high/critical production advisory not on a dated allowlist (two postcss advisories inside Next, until 2026-12-31); Next 15.5.26; `deepmerge-ts` overridden to 8.0.2; CI step | `scripts/audit-deps.mjs`, `package.json`, `verify.yml` | Low | the command, red before the upgrades | FIXED |
| D6 | P3 | Today buries new enquiries under deal warnings | Ranking in one list | Two lists: people first (five), then "Deals to keep moving" (three) | `routers/today.ts`, today page | Low | `browser:screens`, `check:journeys` | FIXED |
| D7 | P3 | Listings rows crowded with five actions | Layout | One primary action; the rest are quiet text buttons; the owner button is pink only when there is no owner | listings page | Low | `browser:screens`, screenshot | FIXED |
| D8 | P3 | Pipeline staleness shown on every card | Not a threshold bug: the demo data had aged past every limit | Seed re-anchors time-in-stage every run (D3) | seed | Low | 14 of 36 flagged after re-seed | FIXED |
| D9 | P3 | Team "Remove" is the loudest element | Styling | Quiet grey row action; the confirmation is unchanged | team | Low | screenshot | FIXED |
| D10 | P3 | Search does not show what it understood | UI | "Read as …" line under the search box (English and Arabic) | command palette | Low | `browser:palette` | FIXED |

## Found by the second, independent audit (26 September)

An agent with no part in the remediation re-tested every row above over
HTTP, in a browser and in SQL, drove the five journeys itself and scored
the product at 64/100. It confirmed the fixes and found the rows below.
Each is fixed with an assertion proved red at its own line, except where
marked.

| ID | Sev | Finding | Root cause | Solution | Files | Risk | Test | Status |
|---|---|---|---|---|---|---|---|---|
| N1 | P1 | An agent's "Nobody's" tab listed every unassigned lead, and bulk could archive, tag or move them all | `leadWhere` spread the caller's scope beside the filters; `filter: "unassigned"` set `assignedToId: null` over the agent's own id | The scope is its own `AND` condition that no filter key can reach | `routers/leads.ts` | Low | `check:lead-lists`: list and bulk on "Nobody's" as an agent, both red with the old spread | FIXED |
| N2 | P2 | Any agent could accept, counter or present a colleague's offer, tick a colleague's deal to completion, or name any lead (even another brokerage's id) on an offer | Offer and deal mutations looked records up by id only; `Offer.leadId` has no foreign key | `lib/deals/scope.ts`: an agent acts only on offers they recorded, whose buyer or property is theirs, or deals they share commission on; managers on anything in the brokerage; the buyer named on an offer must be one the caller can see. Reading stays brokerage-wide on purpose | `lib/deals/scope.ts`, `routers/offers.ts`, `routers/deals.ts` | Medium | `check:journeys` (a colleague: create, counter, accept, present, step — each NOT_FOUND, each red when its guard is removed) | FIXED |
| N3 | P2 | Viewers and compliance officers saw commission forecast and pipeline value in KPIs | `reports.kpis` needed only `lead:read:all` | Needs `revenue:read`, as the revenue screen does; the panel hides itself when refused | `routers/reports.ts` | Low | `check:journeys` viewer assertion, red with the old permission | FIXED |
| N4 | P2 | A completed deal left the property UNDER_OFFER and open to offers | Completion moved the buyer, not the listing | SOLD (or LET for a rental) on completion, only from the live states | `routers/deals.ts` | Low | `check:journeys`, red without it | FIXED |
| N5 | P2 | An agent's own walk-in was handed to the routing rotation, silently | `leads.create` routed every lead | A lead an agent enters is theirs; a manager's still routes, and the form says who it went to | `routers/leads.ts`, `leads/add-lead.tsx` | Low | `check:journeys`, red with routing forced | FIXED |
| N6 | P2 | Saved views took any object (an 8 MB one was saved); a viewer could share a view with the team | `z.record(unknown)`; sharing gated on `lead:read:all` | At most 20 short plain values and 4 KB; sharing and removing team views need `lead:assign` | `routers/views.ts` | Low | `check:lead-lists`, both red with the old schema | FIXED |
| N7 | P3 | Leads and Listings were not in the desktop top bar | Seven-item bar held Blackbook and Offers | Leads and Listings in the bar; Blackbook and Offers in the second tier | `layout/nav.ts` | Low | screenshot, `browser:screens` | FIXED |
| N8 | P3 | Leads could be assigned to a viewer or compliance officer | Only membership was checked | The person must hold `lead:update`; the picker lists only people who can work leads | `lib/leads/assign.ts`, `routers/leads.ts`, leads page | Low | `check:lead-lists`, red without the check | FIXED |
| N9 | P3 | Blackbook search missed local-format numbers and linked people | Raw digits compared; linked entries have no name of their own | Phone compared on the national key; matching leads' ids included | `routers/blackbook.ts` | Low | `check:lead-lists`, red without the key | FIXED |
| N10 | P3 | The VIEWER role lists leads but cannot open Today or a person | The person page and its panels needed `lead:read:own`, which a viewer does not hold | A person can be opened with `lead:read:own` or `lead:read:all` (`requireAnyPermission`); the row scope still decides which; the page hides every editing control from a role that cannot edit; Today's refusal now links to Leads, Listings, Pipeline and Find anyone | `api/trpc.ts`, blackbook/leads/requirements/plans routers, person page, today page | Low | `check:journeys` (viewer reads a person, canEdit false, update refused), red with the old gate | FIXED |
| N11 | P3 | The team page said everybody was "not signed in yet" | `lastSeenAt` was written only by the sign-in event | Written with the session's last-used time, five-minute throttle | `api/context.ts` | Low | `check:two-step`, red without it | FIXED |
| N12 | P3 | Checks leave agents, test listings and wiped documents in the demo brokerage | Browser checks act in the seeded brokerage; clean-up ran only on success, and two checks deleted its documents | Routing and availability clear earlier runs' agents first; add-property removes its test listing; documents and blocking put the documents back; the seed removes any leftovers | check scripts, `prisma/seed.ts` | Low | reseeded: 4 documents, 4 people, no test listings; the three checks pass | FIXED |
| N13 | P3 | "Who wants this property" showed no budget for buyers whose budget is on their requirement | Budget read from the lead | From the matching requirement, lead as fallback | `matching/buyers.ts` | Low | `check:requirements`, red with the old line | FIXED |
| — | P3 | Import over the row limit said "20000 characters" | zod's default message | Says rows, and what to do | `routers/imports.ts` | Low | read | FIXED |
